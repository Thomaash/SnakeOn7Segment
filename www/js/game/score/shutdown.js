define( [], function () {
    return function () {
        // Delete UI
        game.vars.ui.elements.centerScreenText = null;

        // Delete segments
        game.vars.segments = null;
    };
} );